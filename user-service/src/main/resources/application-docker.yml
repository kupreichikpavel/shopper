spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${POSTGRES_DB:shopper}
    username: ${POSTGRES_USER:shopper}
    password: ${POSTGRES_PASSWORD:shopper}

  data:
    redis:
      host: redis
      port: 6379
